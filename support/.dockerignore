# don't copy assets - use the ones that are packed
public/assets
public/packs

# don't copy node_modules - use the ones yarn installed
node_modules/

# ignore file used to store secrets
docker_support/.env
